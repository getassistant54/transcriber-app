meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 20

build:
  commands:
    - npm install
    - npm run build
  artifacts:
    - dist

run:
  command: node dist/server.cjs
  containerPort: 3000
